# Runtime dependencies. Pinned loosely on purpose: this is an application you
# deploy, not a library you import. `pip install -e .` reads pyproject.toml.
aiogram>=3.15,<4
aiohttp>=3.10
aiosqlite>=0.20
pydantic>=2.8
pydantic-settings>=2.4
